/**
 * this is non-complete text based application for first iteration of the final project
 * this is just what I think of this project, not what we gonna do 
 * if u have better methods or ideas, feel free to modify it or completely change this file
 */

import java.util.Scanner;// use scanner to take the location of the chess from user


//I create a class called Gomoku, but we can divide this into more classes if that's the requirement of the assignment.
public class Gomoku {
	public static Scanner scanner = new Scanner(System.in);
	public static int row;//user input location for xcoord
	public static int col;//user input for ycoord
	public int x;//I think we will use these vars later in the project
	public int y;
	public static char[][] board=new char[15][15];//board object 15*15
    //public static char emptySpot='O', and we will replace those empty spots with X and Y
	//player X, his chess symbol will be X
	//player Y, his chess symbol will be Y

	public static void main(String[] args) {
		Board();
		Play();
	}
	
	public static void Board() {
		//print the board
		System.out.println(" now the board looks like this:");// print a new board everytime a player does something.
		for(int i=0;i<15;i++) {
			System.out.println();	
			for(int j=0;j<15;j++) {
				System.out.print(board[i][j] +" O");
			}
		}
		System.out.println();
	}
	
	
	
	public static void Play() {
		//this play method will place player's chess on proper location, and make sure the input is valid
		// we can divide this methods into multiple methods or even classes.
		
		boolean playing=true;
		int i=0;
		int j=0;
		while(true) {
			//i plan to use this while loop to make sure the input is valid.
	
			String filled=playing ?"X":"Y";   //this line of code suppose to 
			System.out.println("player "+filled+"'s turn,"+" please enter the location u want");
			System.out.println("please enter the location you want your chess to be placed (for exmple: 5  5 means place the chess on 5th row, 5th col):");
			row=scanner.nextInt();
			col=scanner.nextInt();
			if(row<=0&&col<=0) {
			System.out.println("out of range,plz re-enter");
			continue;
			}
			if(("board[i][j]".equals("Y")||("board[i][j]".equals("X")))){
				System.out.println("this place is token");
				continue;
			}
			Board();
		}
		}
		
			//if (!checkWin(r,c)) {
				//break;
		//	}
			//playing=!playing;
		//}
		//Board();
	//}
	
	

	/**
	  * TODO
	  * TODO
	  * TODO
	  * TODO
	  *
	  * @Title: turn
	  * @Description: TODO
	  * @param     
	  * @return void   
	  * @throws
	  */
	
	
	 public static boolean checkWin(int x,int y){   	
			if(checkDiagonal(x,y) || checkRow(x,y) || checkCol(x,y)) {
				return true;
				}
			    return false;

		}

	 
	public static boolean checkDiagonal(int x, int y) {
		return true;
	}
		
		
	

	public static boolean checkCol(int x, int y) {
		//todo
		return true;
	}

	public static boolean checkRow(int x, int y) {
		//todo
		return true;
	}
	
	
	public void resultPerformed() {
		 // I think we will print the result tie, X win, Y win in this methods
	 }
	
	}


